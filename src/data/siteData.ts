import { SiteData, SiteDataNew } from '@/types/types';
import jsonData from './siteData.json';
import jsonDataNew from './siteDataNew.json';

export const siteData: SiteData = jsonData as SiteData;

export const siteDataNew: SiteDataNew = jsonDataNew as SiteDataNew;
